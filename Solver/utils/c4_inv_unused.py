# This is not necessary with Numpy just use the below code whenever needed
# import numpy as np
#
#
# h, w = nested_array.shape[:2] (Optional)
# assert (h == 3 and w == 3, 'Cell array is not 3x3') (Optional)
# inv = np.linalg.inv(input)
